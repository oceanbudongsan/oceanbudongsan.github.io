# ── 토큰이 실수로 올라가지 않게 ──────────────────────────
설정.json
config.json
*.env
*.token

# ── 백업·임시 파일 ─────────────────────────────────────
*.geobak*
*.bak
*.bak-*
_백업*/
*_백업_*/

# ── 윈도우·맥이 만드는 잡파일 ───────────────────────────
Thumbs.db
desktop.ini
.DS_Store

# ── 쓰지 않는 사진 ─────────────────────────────────────
gz6mpqCCbf6sv8_*.jpg

# ── 올리기 도우미 (내 컴퓨터에서만 씀) ──────────────────
올리기.bat
